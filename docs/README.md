# docs/

Internal documentation for this repository. Files here are excluded from the published
Jekyll site (see the `exclude:` list in `_config.yml`) and are never served publicly.

- `ai-content-workflows.md` - the canonical, source-of-truth instructions for the two
  AI content workflows (Agentic Engineering lessons, and AI Daily Reads). This is the
  file to read from and to update whenever the workflow instructions change.
